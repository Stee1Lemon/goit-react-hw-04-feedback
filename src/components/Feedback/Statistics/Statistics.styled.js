import styled from 'styled-components';

export const StatisticsField = styled('ul')(() => {
  return {
    '& li': {
      fontSize: 18,
      fontWeight: 500,
      marginBottom: 8,
    },
  };
});
